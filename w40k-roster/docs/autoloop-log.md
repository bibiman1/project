# 自動改善ループ ログ

このファイルは、自動改善ループ（Web調査→実装との比較→改善実装→自己検証→マージ→次サイクル）の各サイクルが読み書きする引き継ぎメモ。各サイクルは記憶を持たない新規セッションとして起動するため、「何を調べたか」「何を実装したか」「次に何を調べるべきか」はすべてここに記録し、次のサイクルはここを読んでから作業を始める。

**大原則**: `docs/rules-notes.md` に記載済みの公式PDF（コアルール・陣営パック）由来の情報は、Web調査の結果より常に優先する。Web調査で複数の情報源が食い違い、かつ`docs/rules-notes.md`にも記載がない場合は、実装を変更せず「要確認」としてこのログに記録するにとどめ、コードは変更しない（不確実な情報を確信度なく実戦投入しない）。

## 運用ポリシー（2026年9月21日、ユーザー承認済み）

- 調査→比較→実装→自己検証→コミット→PR→**マージ**までを人間の確認なしで自動実行してよい。
- 復元ポイント: GitHubブランチ `restore-point/pre-autoloop-2026-09-21`（コミット `7a16c3f`）。何か問題が起きた場合、ユーザーの指示のもとここに戻せる。
- 1サイクルにつき変更は小さく絞る（大規模リファクタや無関係な変更はしない）。
- 既存のgitワークフロー（`claude/stoic-gates-22iefn`ブランチをorigin/mainにrebaseしてからコミット、force-with-leaseでpush）に従う。
- Playwrightでの自己検証を経ないままマージしない。

## 次回調査候補

- [ ] `js/detachments.js`内の各デタッチメント（コホート・サイバネティカ以外の9件）のルール・策略・強化テキストを、11版アデプトゥス・メカニカス陣営パックの内容と突き合わせ、不一致があれば修正する。
- [ ] コホート・サイバネティカの`ruleModifiers`（レギオ・サイバネティカ・キーワードで移動+2/確保+1）以外のデタッチメントにも、同様の「単純な数値バフ」に該当するものがないか調査し、あれば`ruleModifiers`として自動反映する。
- [ ] 命令教条（Doctrina Imperatives）のバトルライン隣接条件（現在は「要手動反映」のメモ表示のみ）を、何らかの形でUI上サポートできないか検討する（例: ユニットに「バトルライン隣接」チェックボックスを追加し、条件付き効果もトラッカーで自動計算する等）。
- [ ] コアルールのアビリティ一覧（`docs/rules-notes.md`の「コアアビリティ一覧」）のうち、ゲームプレイに数値影響があるもの（会心ヒット/会心ウーンズ/連続命中など）について、武器アビリティ欄からの自動判定を追加する価値があるか検討する（現状は自由記述で自動判定なし）。

## 完了ログ

（まだ実行サイクルなし）
